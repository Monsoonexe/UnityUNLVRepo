# made by Richard osborn

for(int i = 0; i < 100; --i)
{
	//do something 100 times
	//no, infinite loop still.
	MyFunction(5);
}

public void MyFunction(int number)
{
	//Console.WriteLine(number); // write number to console.
	
	System.WriteString(number); // super experimental C# hacking.
}
